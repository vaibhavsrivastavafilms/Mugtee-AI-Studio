/**
 * Quick Cut V2 — scene-video path when Studio plan allows.
 * Storyboard still images always run — MP4 export requires rendered frames per scene.
 */
export const QUICK_CUT_V2_TEXT_TO_VIDEO = true

/** Quick Cut V2 — premium black + gold generation UI. */
export const QUICK_CUT_V2_UI = true
